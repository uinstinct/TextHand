import { db } from "./init";

export function initializeDB(val) {
    //db.table("preferences")
    //    .add(val)
    //    .then(c => {
    //        console.log(c);
    //    })

    console.log('do repair');
}